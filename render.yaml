# Render Blueprint（也可在 Dashboard 手动填写相同 Build / Start）
services:
  - type: web
    runtime: python
    name: myapi
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.0"
